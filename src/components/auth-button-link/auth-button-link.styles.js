import { makeStyles, useTheme } from "@material-ui/core/styles";
const authButtonLinkStyles = makeStyles((theme) => ({
  root: {
    marginTop: "50px",
  },
}));

export default authButtonLinkStyles;
