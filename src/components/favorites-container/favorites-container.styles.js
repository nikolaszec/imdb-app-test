import { makeStyles } from "@material-ui/core/styles";
const favoritesContainerStyles = makeStyles((theme) => ({
  wrapepr: {
    padding: "5% 2%",
  },
}));

export default favoritesContainerStyles;
